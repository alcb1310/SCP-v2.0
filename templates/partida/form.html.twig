{% extends "base.html.twig" %}

{% block body %}
     <h3>Partida: </h3>
     {{ form_start(form) }}
          {{ form_widget(form) }}
          <input type="hidden" name="referer" value="{{ referer }}">
          <a href="{{ referer }}" class="btn btn-primary float-end ms-3">Regresar</a>
          <button type="submit" class="btn btn-primary float-end ">Grabar</button>
     {{ form_end(form) }}
{% endblock %}