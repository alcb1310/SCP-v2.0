{% extends "base.html.twig" %}

{% block body %}
     <h3>Partida: </h3>
     {{ form_start(form) }}
          {{ form_widget(form) }}
          <a href="{{ path('partida_show') }}" class="btn btn-primary float-end ms-3">Regresar</a>
          <button type="submit" class="btn btn-primary float-end ">Grabar</button>
     {{ form_end(form) }}
{% endblock %}